# Meds Tracker — Medication Selector

Open `index.html` in your browser to view the demo.

Quick local server (from the project folder):

```bash
python -m http.server 8000
# then open http://localhost:8000 in your browser
```
